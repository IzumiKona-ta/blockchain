package com.example.blockchain.controller;

import org.springframework.stereotype.Controller;

@Controller
public class NotificationController {
    // 简单的控制器，用于处理来自客户端的消息（如果需要）
    // 目前主要是服务端推送，所以这里可以留空，或者处理订阅事件
}
