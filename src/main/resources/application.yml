server:
  port: 8080

fabric:
  # ✅ 关键配置：直连 test-network 里的实时文件
  networkConfigPath: ${user.home}/hyperledger/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/connection-org1.yaml
  
  # ✅ 证书路径
  certificatePath: ${user.home}/hyperledger/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/signcerts/User1@org1.example.com-cert.pem
  
  # ✅ 私钥目录 (自动查找 _sk 文件)
  privateKeyPath: ${user.home}/hyperledger/fabric-samples/test-network/organizations/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/keystore/
  
  mspid: Org1MSP
  username: User1
  channelName: mychannel
  contractName: evidence
